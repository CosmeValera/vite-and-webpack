# VITE HOST and WEBPACK REMOTE

## 🚀 Getting Started
1. Install dependencies in `vite-host` and `webpack-remote`:
```bash
npm i
```
2. Run `vite-host`:
```bash
npm run dev
```
3. Run `webpack-remote`
```bash
npm run start
```